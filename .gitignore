# ---- Secrets: NEVER commit ----
# config.py holds real WiFi credentials on a working device. The repo ships
# config.example.py instead; copy it to config.py during setup.
config.py

# Credentials written by the captive setup portal at runtime
wifi.json

# ---- Runtime files created on the ESP32 (not source) ----
settings.json
events.log

# ---- Build artifacts ----
build/
*.mpy

# ---- Local scratch ----
verify_tmp/
tls_test.py

# Python
__pycache__/
*.pyc

# Editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
